import { toggleDarkMode, toggleMenu, toggleSplash } from './UI/navigation/navigationSlice';
import { setPaletteData, setActiveEditor } from './UI/paletteEditor.slice';

export {
  setActiveEditor,
  setPaletteData,
  toggleDarkMode,
  toggleMenu,
  toggleSplash,
}